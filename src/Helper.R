library(tcltk)
library(R6)

Merger <- R6Class("Helper", public = list(
  merge = function(data_list) #parameter should be a list of csv files
  {
    # with the list of csv files, identify each of them,
    # Afterwards merge them together into one csv file
  }
))




