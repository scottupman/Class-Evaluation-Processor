library(tcltk)
library(R6)

Merger <- R6Class("Merger", public = list(
  merge = function(data_sheet_vector) #parameter should be a vector of csv files
  {
    # with the list of csv files, identify each of them,
    # Afterwards merge them together into one csv file
  }
))




